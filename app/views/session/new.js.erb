// jquery target to empty out the container div in the app layout file.
// rails renders the partial then jquery appends the form to the container.
$('#container').fadeOut('fast');
$('#container').empty();
$('#container').hide().append('<%=j render :partial =>'form'%>');
$('#container').slideDown('slow');

